% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multisample.R
\name{fit_multisample_communication_lmm}
\alias{fit_multisample_communication_lmm}
\title{Fit patient-random-intercept linear mixed models}
\usage{
fit_multisample_communication_lmm(
  prepared,
  design,
  coefficient,
  patient_col = NULL,
  min_units = 6L,
  min_nonzero = 2L,
  max_zero_fraction = 0.98,
  min_response_sd = 1e-12,
  rank_tolerance = 1e-10,
  adjust_method = "BH",
  REML = FALSE,
  standardize_response = TRUE,
  optimizer = "bobyqa",
  optimizer_fallbacks = c("nloptwrap", "Nelder_Mead")
)
}
\arguments{
\item{prepared}{A result from prepare_multisample_communication(...,
unit = "sample").}

\item{design}{A right-hand-side fixed-effect formula, for example
`~ condition + tma`.}

\item{coefficient}{Name or index of the fixed-effect coefficient to test.}

\item{patient_col}{Patient identifier column in prepared$unit_metadata.}

\item{min_units}{Minimum number of slice-level units.}

\item{min_nonzero}{Minimum units with positive communication score.}

\item{max_zero_fraction}{Maximum allowed fraction of zero scores. Set to 1
to disable this support screen while still recording zero_fraction.}

\item{min_response_sd}{Minimum standard deviation on the log1p-score scale.
Features below this threshold are marked insufficient_variation.}

\item{rank_tolerance}{Tolerance passed to the fixed-effect design rank check.}

\item{adjust_method}{Multiple-testing correction method.}

\item{REML}{Whether to use restricted maximum likelihood. Fixed-effect
comparisons generally use `FALSE`.}

\item{standardize_response}{Whether to standardize each feature response
before fitting. Estimates and standard errors are returned on the original
log1p-score scale; this can improve conditioning for very small scores.}

\item{optimizer}{lme4 optimizer used for each feature.}

\item{optimizer_fallbacks}{Additional optimizers tried after \code{optimizer}.
A successful warning-free fit is preferred; otherwise the successful fit
with the first available optimizer is retained and flagged.}
}
\value{
Feature-level mixed-model estimates, standard errors, Wald
  statistics and p-values, plus optimizer, variance-component,
  convergence, singular-fit and failure diagnostics, including the attempted
  optimizer sequence. BH-adjusted p-values are reported only for fits with
  status equal to ok.
}
\description{
Fits one linear mixed model per communication feature while retaining one
row per spatial slice. The response is log1p(probability), the fixed
effects are supplied by `design`, and `patient_col` defines a random
intercept for patient-level correlation among slices.
}
\details{
The returned p-values are Wald normal-approximation p-values. They are
intended for a transparent, lightweight screening analysis; confirmatory
analyses may use lmerTest or parametric bootstrap degrees of freedom.
}
