% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytic.R
\name{experimental_analytic_cellchat_group_support}
\alias{experimental_analytic_cellchat_group_support}
\title{Experimental analytic significance for simple CellChat LR records}
\usage{
experimental_analytic_cellchat_group_support(
  prepared,
  group,
  group_support,
  components,
  blocks,
  Kh = 0.5,
  n = 1,
  min_group_size = 30L,
  density_window = 0.05,
  max_tie_fraction = 0.1,
  max_zero_fraction = 0.1,
  max_abs_influence_correlation = 0.8,
  min_log_variance = 1e-12,
  max_influence_entries = 5e+07
)
}
\arguments{
\item{prepared}{A result from [prepare_sparse_trimean()].}

\item{group}{Fixed target-group labels.}

\item{group_support}{Spatially supported group pairs.}

\item{components}{Resolved CellChat LR components.}

\item{blocks}{Spatial exchangeability blocks. Exactly one block is required
by this first homogeneous-null prototype.}

\item{Kh, n}{Positive Hill-function parameters used for the observed score.}

\item{min_group_size}{Minimum cells in both sender and receiver groups.}

\item{density_window}{Probability half-width used for quantile-density
estimation.}

\item{max_tie_fraction}{Maximum mass allowed at any triMean quartile.}

\item{max_zero_fraction}{Maximum zero fraction allowed in either expression
profile before requiring permutation fallback.}

\item{max_abs_influence_correlation}{Dependence guard for ligand and
receptor influence functions.}

\item{min_log_variance}{Minimum delta-method variance.}

\item{max_influence_entries}{Maximum cells times candidate genes retained in
the influence-function cache.}
}
\description{
This prototype uses a joint influence-function approximation for the
ligand and receptor triMeans under one homogeneous finite-population label
permutation null. The covariance calculation retains both within-profile
ligand-receptor dependence and the negative dependence induced when two
different groups partition the same cells. Records outside the validated
simple-LR domain are returned with an explicit fallback reason and no
analytic p-value.
}
\details{
This function is intentionally conservative and is not yet the default
SpatialESS inference mode. In particular, spatially stratified blocks,
autocrine records, complexes, cofactors, strong dependence, small groups,
zero quartiles and heavily tied quartiles require permutation fallback.
}
