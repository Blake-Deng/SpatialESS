# Minimal example data

The public package does not include a large expression matrix. Use the small
objects in the package tests or provide a gene-by-cell Matrix, aligned cell
coordinates and group labels as shown in the root README.

Large benchmark objects, raw matrices and official reference outputs are
excluded from Git. They are referenced by provenance manifests and archived
separately when redistribution is permitted.
